[features]
multi_agent = true

[agents]
max_threads = 4

[agents.default]
description = "General-purpose helper."

[agents.explorer]
description = "Fast codebase explorer for read-heavy tasks."

[agents.example]
description = "An example agent to show the format. Replace with your own."
config_file = ".codex/agents/example.toml"

[agents.storebrand-bills]
description = "Pays bills via Storebrand personal account using the openclaw browser."
config_file = ".codex/agents/storebrand-bills.toml"
