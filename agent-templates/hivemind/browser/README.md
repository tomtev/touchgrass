# Browser

Shared browser profile directory. Agent-specific profiles live in `agents/<name>/browser/`.

## Usage

```bash
agent-browser --profile .profile --headed --executable-path "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" open <url>
```

Install with `npm install -g @vercel/agent-browser`.
